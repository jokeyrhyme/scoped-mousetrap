/*jslint indent:2, maxlen:80*/
/*global suite, test*/ // mocha
/*global assert*/ // chai

suite('scoped-mousetrap', function () {
  'use strict';

  test('abc', function () {
    assert(true);
  });

});
